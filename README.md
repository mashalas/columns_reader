# columns_reader
Чтение столбцов файлов в формате csv (comma-separated values) и tsv (tab-separated values) в двумерный массив (список python) и базовые операции с прочитанными данными: добавление удаление столбцов, конвертирование в целый/дробный/временной типа, OneHotEncoding
